########################################################################
#
# mappings executable paths and alias for constant name in scripts
# and varying build versions.  MAPPINGS can run with or without
# environment variables, but this allows for more flexible naming and
# advanced runtime configurations, see advanced installs in
# the documentation.
#
# Syntax for csh, tcsh and csh family of shells, Put in your startup
# for the appropriate shell for both interactive and non-interactive
# shells most commonly .tcshrc as or .cshrc
#
########################################################################
#
# Manual:
#
# set general mappings area then specify data/ location & mappings exe
#
# or globally installed in system base area or other user area:
# up to user to use sudo make install su install options in make
# or create other locations
# set mapbase = "/usr/local"
# setenv MAPDATA  "$mapbase/share/mappings"
# set mapbin  = "$mapbase/bin"
# setenv MAPBIN "$mapbin"
#
# add bin area to global path, same for all
#
# set path = ($mapbin $path)
#
########################################################################
#
# Standard:
#
